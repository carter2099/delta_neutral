# TODO

- [ ] Add a job activity/error log visible in the UI so users can see when background jobs (HedgeSyncJob, WalletSyncJob, PositionSyncJob) encounter errors like rejected orders, API failures, or missing env vars — currently these errors are only visible in server logs
