<!DOCTYPE html>
<html>
<head>
  <style>
    body { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Helvetica, Arial, sans-serif; background: #0d1117; color: #e6edf3; padding: 20px; }
    .card { background: #161b22; border: 1px solid #30363d; border-radius: 6px; padding: 20px; max-width: 600px; margin: 0 auto; }
    .header { font-size: 24px; font-weight: 600; margin-bottom: 20px; color: #f85149; }
    .error { background: rgba(248, 81, 73, 0.15); border: 1px solid #f85149; border-radius: 6px; padding: 12px; margin-bottom: 20px; color: #f85149; }
    .label { font-size: 12px; color: #6e7681; text-transform: uppercase; margin-bottom: 4px; }
    .value { font-size: 16px; font-family: "SF Mono", monospace; margin-bottom: 12px; }
    .btn { display: inline-block; padding: 10px 20px; background: #58a6ff; color: white; text-decoration: none; border-radius: 6px; }
    code { background: #0d1117; padding: 8px; border-radius: 4px; display: block; font-size: 14px; overflow-wrap: break-word; }
  </style>
</head>
<body>
  <div class="card">
    <div class="header">Rebalance Failed</div>

    <div class="error">
      Rebalance failed for <%= @position.token0_symbol %>/<%= @position.token1_symbol %>
    </div>

    <div class="label">Position</div>
    <div class="value"><%= @position.token0_symbol %>/<%= @position.token1_symbol %> on <%= @position.network.capitalize %></div>

    <div class="label">Trigger</div>
    <div class="value"><%= @event.trigger_type.capitalize %></div>

    <% if @event.error_message.present? %>
      <div class="label">Error</div>
      <code><%= @event.error_message %></code>
    <% end %>

    <p style="margin-top: 20px; color: #8b949e; font-size: 14px;">
      Please check the error and try again. If the problem persists, check the circuit breaker status in Settings.
    </p>

    <p style="margin-top: 20px;">
      <a href="<%= rebalance_event_url(@event) %>" class="btn">View Details</a>
    </p>
  </div>
</body>
</html>
