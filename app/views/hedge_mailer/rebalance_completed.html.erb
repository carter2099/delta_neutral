<!DOCTYPE html>
<html>
<head>
  <style>
    body { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Helvetica, Arial, sans-serif; background: #0d1117; color: #e6edf3; padding: 20px; }
    .card { background: #161b22; border: 1px solid #30363d; border-radius: 6px; padding: 20px; max-width: 600px; margin: 0 auto; }
    .header { font-size: 24px; font-weight: 600; margin-bottom: 20px; }
    .success { background: rgba(63, 185, 80, 0.15); border: 1px solid #3fb950; border-radius: 6px; padding: 12px; margin-bottom: 20px; color: #3fb950; }
    .label { font-size: 12px; color: #6e7681; text-transform: uppercase; margin-bottom: 4px; }
    .value { font-size: 16px; font-family: "SF Mono", monospace; margin-bottom: 12px; }
    .btn { display: inline-block; padding: 10px 20px; background: #58a6ff; color: white; text-decoration: none; border-radius: 6px; }
    .badge { display: inline-block; font-size: 12px; padding: 2px 8px; border-radius: 4px; background: rgba(88, 166, 255, 0.2); color: #58a6ff; }
  </style>
</head>
<body>
  <div class="card">
    <div class="header">
      Rebalance <%= @event.paper_trade? ? 'Simulated' : 'Completed' %>
      <% if @event.paper_trade? %>
        <span class="badge">Paper Trade</span>
      <% end %>
    </div>

    <div class="success">
      Rebalance completed successfully for <%= @position.token0_symbol %>/<%= @position.token1_symbol %>
    </div>

    <div class="label">Position</div>
    <div class="value"><%= @position.token0_symbol %>/<%= @position.token1_symbol %> on <%= @position.network.capitalize %></div>

    <div class="label">Trigger</div>
    <div class="value"><%= @event.trigger_type.capitalize %></div>

    <% if @event.duration %>
      <div class="label">Duration</div>
      <div class="value"><%= number_with_precision(@event.duration, precision: 2) %>s</div>
    <% end %>

    <% if @event.executed_actions.any? %>
      <div class="label">Actions <%= @event.paper_trade? ? 'Simulated' : 'Executed' %></div>
      <% @event.executed_actions.each do |action| %>
        <div class="value">
          <%= action["asset"] %>: <%= action["action"] %> <%= number_with_precision(action["size"] || 0, precision: 6) %>
        </div>
      <% end %>
    <% end %>

    <p style="margin-top: 20px;">
      <a href="<%= rebalance_event_url(@event) %>" class="btn">View Details</a>
    </p>
  </div>
</body>
</html>
